# Netlify configuration for CivicForge Frontend

[build]
  publish = "dist"
  command = "npm run build"

# SPA Fallback rule for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
